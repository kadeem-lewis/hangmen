services:
  - type: web
    name: hangmen
    env: node
    branch: main
    startCommand: node .output/server/index.mjs
    buildCommand: pnpm i --shamefully-hoist && pnpm build
    envVars:
      - key: NITRO_PRESET
        value: render_com
